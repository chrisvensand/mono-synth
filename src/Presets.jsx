import React from 'react';
import './index.css';

export default class Presets extends React.Component {
    render() {
        return (
            <div className="presets">          
            </div>
        );
    }
}